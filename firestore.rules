rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Permitir acceso total para prototipado rápido.
    // El agente ha solicitado un redespliegue para asegurar la consistencia.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
